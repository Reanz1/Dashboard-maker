services:
  dashboard:
    image: randomsi/dashboard-maker:latest
    container_name: dashboard-maker
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data
      - ./images:/images
