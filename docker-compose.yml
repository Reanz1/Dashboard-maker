services:
  dashboard:
    image: randomsi/dashboard-maker:latest
    container_name: dashboard-maker
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      # services.json, categories.json, config.json — and custom themes
      # in ./data/themes
      - ./data:/data
      # service icons
      - ./images:/images
